<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit User's Information</title>

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.4/dist/jquery.slim.min.js"></script>

    <!-- Popper JS -->
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <div class="container">
        <div class="row">
            <form action="/put-doctor" method="POST">
                <div class="form-row">
                    <div class="col-12 mt-3"><b>Edit Doctor's Information:</b></div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="inputFirstname4">First Name</label>
                        <input 
                          type="text" 
                          class="form-control" 
                          placeholder="First Name" 
                          name="firstName"
                          value="<%= doctor.firstName %>"
                          required
                        />
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputLastname4">Last Name</label>
                        <input
                          type="text" 
                          class="form-control"
                          placeholder="Last Name" 
                          name="lastName"
                          value="<%= doctor.lastName %>"
                          required
                        />
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="inputAddress">Address</label>
                        <input 
                          type="text" 
                          class="form-control" 
                          placeholder="Address" 
                          name="address"
                          value="<%= doctor.address %>"
                          required
                        />
                    </div>
                    <div class="form-group col-md-6">
                        <label for="inputAge">Age</label>
                        <input 
                          type="number" 
                          class="form-control" 
                          placeholder="Age" 
                          name="age"
                          value="<%= doctor.age %>"
                          required
                        />
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label for="inputGender">Gender</label>
                        <select class="form-control" name="gender" required>
                            <option value="" disabled selected>Select Gender</option>
                            <option value= 1 <%= doctor.gender === 'male' ? 'selected' : '' %>>Male</option>
                            <option value= 2 <%= doctor.gender === 'female' ? 'selected' : '' %>>Female</option>
                            <option value= 3 <%= doctor.gender === 'other' ? 'selected' : '' %>>Other</option>
                        </select>
                    </div>
                </div>
                <!-- Thêm trường Clinic -->
                <div class="form-group">
                    <label for="inputClinic">Clinic</label>
                    <select name="clinicId" class="form-control" required>
                      <option value="1">Cơ sở số 1</option>
                      <option value="2">Cơ sở số 2</option>
                      <option value="3">Cơ sở số 3</option>
                      <option value="4">Cơ sở số 4</option>
                      <option value="5">Cơ sở số 5</option>
                    </select>
                  </div>
  
                  <!-- Thêm trường Specialization -->
                  <div class="form-group">
                    <label for="inputSpecialization">Specialization</label>
                    <select name="specializationId" class="form-control" required>
                      <option value=1>Tim mạch</option>
                      <option value=2>Nội khoa</option>
                      <option value=3>Da liễu</option>
                      <option value=4>Răng hàm mặt</option>
                      <option value=5>Sản phụ khoa</option>
                    </select>
                  </div>
                <input name="id" value="<%= doctor.id %>" type="hidden"/>
                <button type="submit" class="btn btn-primary">Update</button>
            </form>
        </div>
    </div>
</body>
</html>
